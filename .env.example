# Upstash Redis (for optional conversation history).
#
# These are safe placeholders. Add the real values in Vercel environment variables.
UPSTASH_REDIS_REST_URL="YOUR_UPSTASH_REDIS_REST_URL"
UPSTASH_REDIS_REST_TOKEN="YOUR_UPSTASH_REDIS_REST_TOKEN"

# CLAUDE_API_KEY intentionally omitted (you said to leave it empty for now).
